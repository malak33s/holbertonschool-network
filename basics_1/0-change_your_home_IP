#!/usr/bin/env bash
# script modifies IP resolution localhost and facebook.com

# Change localhost to resolve to 127.0.0.2
sudo sed -i 's/127.0.0.1/127.0.0.2/' /etc/hosts

# Change facebook.com to resolve to 8.8.8.8
sudo sed -i '/facebook.com/c\8.8.8.8 facebook.com' /etc/hosts